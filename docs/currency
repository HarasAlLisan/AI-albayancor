AI_albayancor/
│
├── docs/
│   ├── currency/
│   │   ├── byn_master.json
│   │   ├── students.json
│   │   ├── teachers.json
│   │   ├── developers.json
│   │   ├── general_users.json
│   │   ├── projects_allocation.json
│   │   └── treasury_balance.json
# مجلد العملة الرقمية BYN – AI albayancor

هذا المجلد يحتوي على جميع الملفات المرتبطة بعملة البيان (BYN) ونظام توزيعها عبر مشروع **AI albayancor**.

---

## 📌 الهدف

- تأسيس عملة رقمية متوازنة وفق النصوص الثابتة (البيان القرآني).
- ضمان عدالة التوزيع لكل مستخدم وفق **هاش ADAM**.
- التحكم بالفائض والمشاريع باستخدام **تردد a1** كفلتر للعدالة والنية.
- توفير مرجع واضح ومنظم لكل الملفات المتعلقة بالعملة.

---

## 🗂️ الملفات الموجودة في المجلد

### 1️⃣ `byn_master.json`
- الملف الرئيسي للعملة BYN.
- يحتوي على:
  - الاسم والرمز واللون والنسخة.
  - إجمالي المعروض والتوزيع بين الفئات.
  - الربط مع الملفات الأخرى (`linked_files`).
  - تردد a1 لضمان التوازن.
  - المراحل التاريخية للملكية والنظام.

### 2️⃣ `students.json`
- بيانات الطلاب.
- كل طالب مرتبط بـ **هاش ADAM** فريد.
- يوضح كمية BYN المخصصة لكل طالب.
- يحتوي على سجل النشاط والمشاريع (اختياري).

### 3️⃣ `teachers.json`
- بيانات المعلمين.
- كل معلم له هاش ADAM فريد.
- يوضح كمية BYN المخصصة لكل معلم.
- مشاريع مرتبطة بكل معلم وسجل النشاط.

### 4️⃣ `developers.json`
- بيانات المطورين.
- هاش ADAM فريد لكل مطور.
- BYN المخصص حسب الدور (core_dev أو مطور عادي).
- ملف العقد الخاص بالمطورين مخفي ومنفصل.
- يوضح المشاريع والسجل والنشاط.

### 5️⃣ `general_users.json`
- بيانات المستخدمين العامين.
- هاش ADAM فريد لكل مستخدم.
- BYN مخصص لكل مستخدم وفق الفئة.
- يمكن تتبع النشاط والمشاريع لكل مستخدم.

### 6️⃣ `projects_allocation.json`
- تخصيص المشاريع المختلفة للفئات.
- يوضح BYN المخصص لكل مشروع.
- مرتبط بتردد a1 لضمان العدالة في التوزيع.
- يوضح حالة المشروع (planned, ongoing, completed).

### 7️⃣ `treasury_balance.json`
- بيت المال العام للعملة BYN.
- يوضح الأصول الكلية والتوزيع.
- سجل العمليات المرتبطة بالهاش ADAM والفئات.
- يضمن التوازن الكامل وفق تردد a1.

---

## ⚖️ الفلسفة الأساسية

- **BYN** هي العملة الأساسية للنظام، مستندة على **النصوص الثابتة**.
- توزيع العملة يتم على جميع المستخدمين (طلاب، معلمين، مطورين، عامين) وفق **هاش ADAM**.
- الفائض يخصص للمشاريع والبيت المال، مع مراعاة الشفافية.
- جميع العمليات متزامنة مع **تردد a1** للتحقق من العدالة والنية.

---

## 📈 الاستخدام

1. استخدام الملفات داخل النظام لتوزيع BYN على الفئات.
2. متابعة المشاريع والفائض عبر `projects_allocation.json`.
3. إدارة بيت المال وسجلات العمليات عبر `treasury_balance.json`.
4. تحديث البيانات عند إضافة مستخدمين جدد أو مشاريع جديدة، مع الحفاظ على التوازن بتردد a1.

---

## 🔗 الربط بين الملفات

```json
"linked_files": {
  "users": ["students.json", "teachers.json", "developers.json", "general_users.json"],
  "projects": "projects_allocation.json",
  "treasury": "treasury_balance.json"
}
